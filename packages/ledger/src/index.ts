/**
 * @pfm/ledger — Double-entry journal engine — the ONLY module allowed to write journal entries (arrives in B2, blueprint §4, §10)
 *
 * B0 scaffold stub: real implementation lands in B2.
 */
export const PACKAGE = "@pfm/ledger";
